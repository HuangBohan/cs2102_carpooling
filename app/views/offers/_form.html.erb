<%= simple_form_for(@offer) do |f| %>
  <% if @offer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@offer.errors.count, "error") %> prohibited this offer from being saved:</h2>

      <ul>
      <% @offer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.input :datetime %>
  <%= f.input :pickUpPoint %>
  <%= f.input :dropOffPoint %>
  <%= f.input :vacancies %>
  <%= f.input :car_id %>
  <%= f.submit %>
<% end %>
